replicaCount: 1

image:
  repository: somanathmuduli03/expensetracker-frontend
  tag: "1.1"
  pullPolicy: Always

imagePullSecrets:
  - name: dockerhub-secret

service:
  type: LoadBalancer
  port: 80
  targetPort: 80

namespace: dev

ingress:
  host: app.afet.online
  tlsSecretName: app-afet-tls
